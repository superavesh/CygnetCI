Add an alarm sound file here named:  alarm.mp3

This is the looping sound played during a critical alert. Any short MP3 works
(1-3 seconds; it loops). The app references assets/alarm.mp3 in pubspec.yaml,
so the file must exist before you build/run.
