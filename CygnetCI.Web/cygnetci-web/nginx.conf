server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Next.js static export uses trailingSlash: true -> folders with index.html.
    # Try exact file, then directory, then .html, then fall back to index.html.
    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }

    # The postbuild step copies this Dockerfile/nginx.conf alongside the static
    # export so `out/` is a self-contained image build context; don't serve them.
    location = /Dockerfile { deny all; return 404; }
    location = /nginx.conf { deny all; return 404; }

    # Never cache the runtime API config so repointing takes effect on refresh.
    location = /system.config.js {
        add_header Cache-Control "no-store, no-cache, must-revalidate";
        try_files $uri =404;
    }

    # Long cache for hashed static assets
    location /_next/static/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    gzip on;
    gzip_types text/plain text/css application/javascript application/json image/svg+xml;
}
